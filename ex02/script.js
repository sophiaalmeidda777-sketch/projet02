let valor = Number(prompt("Qual o valor do pedido?"));
let desconto = valor * 0.10;
let total = valor - desconto;
if (valor > +50); 
  alert("tem o cartao fidelidade?")
  let opcao = prompt ("sim | nao");
  if (opcao == "sim" ) {
    alert("ganhou desconto de 10%")
  }
  if (opcao == "nao") 
    alert("ganhou um cookie")
    and ();
  
  


